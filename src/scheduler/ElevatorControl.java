package scheduler;

public class ElevatorControl implements Runnable {
    private Scheduler scheduler;

    public ElevatorControl(Scheduler scheduler) {
        this.scheduler = scheduler;
    }

    @Override
    public void run() {
        
    }
}
